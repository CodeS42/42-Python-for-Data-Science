def count_in_list(lst, elem):
    """
    Counts the number of times an element appears in a list.

    Args:
        lst: The list in which to search for elements.
        elem: The element to search for in the list.

    Returns:
        lst.count(elem): The number of times an element appears in a list.
    """
    return lst.count(elem)
